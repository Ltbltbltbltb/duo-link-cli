# duo-link runtime

Este diretorio guarda os artefatos locais do canal (`chat.log`, `.chat.lock`, `context.*.md`) e o wrapper executavel `duo-cli`.

A documentacao canonica do projeto fica em [`../README.md`](../README.md).
